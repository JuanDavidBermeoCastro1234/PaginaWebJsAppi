const url = "https://financialmodelingprep.com/stable/search-name?query=AA"

    // Obtener el contenedor desde el HTML donde se va a insertar la tabla
    const contenedor = document.getElementById("contenedor")